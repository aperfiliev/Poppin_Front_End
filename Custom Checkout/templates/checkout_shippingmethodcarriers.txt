<h2>${"Select your delivery method"|translate}</h2>
<div class="f-row f-cb">
	{var count = 0}
	{for method in obj.methods}
		<!--{count++}-->
	{/for}
	{if count}
		<select id="shipCarriers">
			{for method in obj.methods}
				<option value="${method_index}"  {if _m.isCarrierSelected(method_index, obj.selected)}selected{/if}>{if method_index === "ups"}UPS{else}${"Other"|translate}{/if}
			{/for}
		</select>
	{/if}
	
</div>
<div id="shippingRates"></div>		