            <!-- BEGIN sopping cart -->
            <div class="shp-cart-aside clearfix">
                <div class="block-title clearfix">
                    <a href="${obj.cart.carturl}">${"Edit"|translate}</a>
                    <h2>${"Shopping Cart"|translate}</h2>
                </div>
                <div class="cart-scroll">
                    <ul class="cart-list cart-list-aside">
			{for item in obj.cart.items}
	                <li>
                            <div class="thumb">
				{if item.image && item.image != ""}	
					<a href="${item.storeurl}">
					<img src="${item.image}" width="50" alt="${item.name}" />
					</a>
				{else}
					<span class="missing-image"></span>
				{/if}
			    </div>
                            <div class="wrap clearfix">
                                <h3><a href="${item.storeurl}">${item.name}</a></h3>
                                <p>${"Qty"|translate}: ${item.quantity}</p>
								{if !item.promotionamount }
									<p><strong class="price">${item.subtotal|formatCurrency}</strong></p>

								{else}
									<p><strong class="price st-through">${item.subtotal|formatCurrency}</strong>
									   <strong class="price color-r">${item.promotionamount|formatCurrency}</strong></p>
								{/if}
                            </div>
                        </li>
			{/for}		
                    </ul>

                </div>
            </div>
            <!-- END sopping cart -->
