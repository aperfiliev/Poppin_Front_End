<!-- checkout_tmpl_body -->
{if obj.current.id === "confirmation"}
	<div id="checkoutStepDiv"></div>
{else}
	<div class="block checkout-hold clearfix">
		{for step in obj.steps}
			{if step.id !== "confirmation"}
				<div class="checkout-step {if step.visited || step.isActive} step-active {/if} {if step.id === 'review'} no-bg {/if} clearfix">
					<div class="checkout-title clearfix">
						{if step.isComplete}
							<a onclick='BDK.fire(BDK.konst.Events.CHECKOUT_STEP, "${step.id}");' name="saveButton" class="button2">${"Edit"|translate}</a>
						{/if}
						<h2>
							<span>${step.pos}</span>
							{if step.id === "shipping"}
								${"Shipping &amp; Delivery"|translate}
							{elseif step.id === "billing"}
								${"Billing &amp; Payment"|translate}
							{elseif step.id === "review"}
								${"Review Your Order"|translate}
							{else}
								${step.id}
							{/if}
						</h2> 
					</div>
					{if step.isComplete}
						<div id="${step.id}Summary"></div>
					{/if}
					{if step.isActive}
						<div id="checkoutError"></div>
						<div id="checkoutStepDiv"></div>
					{/if}
				</div>
			{/if} 
		{/for}
	</div>
{/if}