{macro createPromoCodeForm(obj)}
{var event = obj.checkout ? "customCheckoutApplyPromo" : "customCartApplyPromo"}
{if obj.checkout}
	<h2 id="applyPromoCodeCheckout"><span>+</span> ${"Apply Promo Code"|translate}</h2>
	{if obj.cart.promocode.hasOwnProperty('promocode')}
		<div class="box-input-code clearfix">
	{else}
		<div class="box-input-code clearfix hidden">
	{/if}
{else}
	<h2 id="applyPromoCodeCart">${"Have a Promo Code?"|translate}</h2>
	<div class="box-input-code clearfix">
{/if}
{if "F" === obj.cart.promocode.isvalid}
	<p>${"Enter your promotional code or coupon code below. Limit one per customer, please."|translate}</p>
	<div class="box-input-wrap clearfix">
		
		<input id="cart-promocode" type="text" class="field" placeholder="${"Promo or coupon code"|translate}" />
		<div class="button2"><input id="applyPromo" type="button" value="${"Apply"|translate}" onclick="BDK.fire('${event}', $('cart-promocode').value);"/></div>
		<br><br>
		<div class="custom-error" id="promoError">
		<!-- <a id="removePromo" onclick="BDK.fire('${event}','${obj.cart.promocode.promocode}');" class="button2">${"Remove"|translate}</a>-->	
		</div>
	</div>


{elseif "T" === obj.cart.promocode.isvalid} 
	<p>${"Your promo code was applied successfully. Limit one code per order. Remove this discount to apply a different code."|translate}</p>
	<div class="box-input-wrap clearfix">
		<strong class="code-applied nowrap">${"Promo code $(0) applied"|translate: obj.cart.promocode.promocode}</strong>
		<a id="removePromo" onclick="BDK.fire('${event}','${obj.cart.promocode.promocode}');" class="button2">${"Remove"|translate}</a>
	</div>
	{else}
		<p>${"Enter your promotional code or coupon code below. Limit one per customer, please."|translate}</p>
		<div class="box-input-wrap clearfix">
			<input id="cart-promocode" type="text" class="field" placeholder="${"Promo or coupon code"|translate}" />
			<div class="button2"><input id="applyPromo" type="button" value="${"Apply"|translate}" onclick="BDK.fire('${event}', $('cart-promocode').value);"/></div>
		</div>
		<div class="custom-error" id="promoError"></div>
	{/if} 
</div>
{/macro}